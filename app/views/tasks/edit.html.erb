<div class="container">
  <button class="btn btn-primary" type="submit"><%= link_to("Back to Task Details", event_task_url(@event, @task)) %></button>
  <h1>Editing Task: <%= @task.name %></h1>
  <%= render partial: 'form' %>
  
</div>
